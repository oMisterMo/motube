/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./public/**/*.{js,jsx,ts,tsx,vue,html}"],
    // Support toggling dark mode manually instead of relying on the operating system preference
    darkMode: "class",
    theme: {
        extend: {},
    },
    plugins: [],
};
